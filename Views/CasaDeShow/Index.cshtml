@model IEnumerable<Casadeshow.Models.CasaDeShow>

@{
    ViewData["Title"] = "Casas de show";
}

<div style="text-align:center;">
    <h1>Casas de show</h1>
</div>

<p>
    <a asp-action="Create">Criar nova casa de show</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                Endereço
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Endereco)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CasaDeShowId">Editar</a> |
                <a asp-action="Delete" asp-route-id="@item.CasaDeShowId">Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
