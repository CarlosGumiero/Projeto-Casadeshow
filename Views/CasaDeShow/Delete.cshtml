@model Casadeshow.Models.CasaDeShow

@{
    ViewData["Title"] = "Excluir casa de show";
}

<h1>Excluir</h1>

<h3>Tem certeza que deseja excluir a casa de show selecionada?</h3>
<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nome):
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nome)
        </dd>
        <dt class = "col-sm-2">
            Endereço:
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Endereco)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="CasaDeShowId" />
        <button type="submit" value="Delete" class="btn btn-danger">Excluir</button> |
        <a asp-action="Index">Voltar</a>
    </form>
</div>
