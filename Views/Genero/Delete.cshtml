@model Casadeshow.Models.Genero

@{
    ViewData["Title"] = "Excluir gênero";
}

<h1>Delete</h1>

<h3>Tem certeza que deseja excluir o gênero?</h3>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nome)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nome)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="GeneroId" />
        <button type="submit" value="Delete" class="btn btn-danger">Excluir</button> |
        <a asp-action="Index">Voltar</a>
    </form>
</div>
